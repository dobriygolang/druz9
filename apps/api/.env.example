POSTGRES_DB=back
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
CONFIG_PROFILE=local

# App bootstrap config now lives in .platform/values_local.yaml / values_prod.yaml.
# Keep .env only for docker-compose interpolation and optional local overrides.
