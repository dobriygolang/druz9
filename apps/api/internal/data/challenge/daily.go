package challenge

// Daily challenge write/read helpers (UpsertDailyResult,
// GetDailyLeaderboard, GetUserDailyScore) used to live here. Their
// only callers were the SubmitDailyReview / GetDailyLeaderboard RPCs
// which were removed as dead — no frontend ever posted a daily score.
// File kept as a placeholder; re-add the three methods if the daily
// challenge page starts accepting submissions.
